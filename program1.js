/**
 * @param {string} s
 * @return {boolean}
 */
var isValid = function(s) {
    let len = s.length();
    if(len %2 !=0){
      return false;
    }
    else { 
       if()
    }


};

module.exports = { isValid };


